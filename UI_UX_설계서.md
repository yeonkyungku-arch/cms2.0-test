# CMS 2.0 UI/UX 설계서

> 작성일: 2026-06-10  
> 대상 브랜치: `claude/funny-lovelace-bW71R`

---

## 설계 철학

이 CMS는 광고 미디어 운영자가 **프로덕트(광고 상품)를 등록하고, 미디어(광고 단말)에 편성을 할당하고, 그 현황을 확인하는** 워크플로우를 중심으로 설계됩니다.

핵심 원칙은 세 가지입니다.

1. **드릴다운 구조** — 목록에서 상세로, 상세에서 편성표로 자연스럽게 파고들 수 있어야 합니다. 사용자가 "지금 내가 어느 맥락에 있는지"를 항상 인지할 수 있어야 하고, 맥락을 유지한 채로 다른 미디어나 프로덕트로 전환할 수 있어야 합니다.
2. **정보 밀도와 가독성의 균형** — 미디어 수가 많아도 한 화면에서 비교하고 선택할 수 있어야 합니다. 동시에 지나치게 압축되어 중요 정보가 누락되어서는 안 됩니다.
3. **불필요한 중복 제거** — 같은 정보가 여러 영역에 반복되면 화면이 복잡해지고 사용자의 시선이 분산됩니다. 정보는 한 곳에서 명확하게 제공합니다.

---

## 1. GNB (Global Navigation Bar)

### 접근 제한 설계 의도

현재 프로토타입은 **프로덕트**와 **편성표** 워크플로우만 완성된 상태입니다. 대시보드, 미디어, 콘텐츠, 모니터링 등 미완성 메뉴에 사용자가 진입하면 빈 화면이나 에러를 마주치게 됩니다. 이는 서비스 신뢰도를 떨어뜨립니다.

미완성 메뉴를 완전히 숨기는 대신 **흐리게 표시(opacity: 0.4)**하는 이유는, 이 시스템이 향후 더 많은 기능을 제공할 것임을 사용자에게 암시하기 위해서입니다. "있는데 아직 열리지 않는" 상태가 "아예 없는" 상태보다 서비스의 방향성을 더 잘 전달합니다.

```
활성: 프로덕트, 편성표
비활성: 대시보드, 미디어, 콘텐츠, 모니터링 (opacity:0.4, pointer-events:none)
```

### 드릴다운 내비게이션 구조 설계 의도

사용자는 항상 "큰 그림 → 세부"의 방향으로 작업합니다. 프로덕트 목록에서 특정 프로덕트를 선택해 상세를 보고, 거기서 편성표로 이동하는 흐름입니다. 편성표 안에서도 다른 미디어나 프로덕트를 즉시 전환할 수 있어야 하며, 이를 위해 편성표 헤더에 드롭다운 선택 UI를 배치했습니다.

```
프로덕트 목록 → 프로덕트 상세 → 프로덕트 편집
                              → 플레이플랜
                              → 월간 편성표 → 일간 편성표
미디어 월간 편성표 → 미디어 일간 편성표
```

---

## 2. 프로덕트 목록 (`product-list.html`)

### 현황판 카드 높이 일치

**의도**: 편성 상태 현황판과 프로덕트 상태 현황판은 같은 계층의 정보를 나란히 제공합니다. 두 카드의 높이가 다르면 시각적으로 비대칭이 발생하고, 사용자가 둘을 동등한 위상의 정보로 인식하지 못합니다.

`align-items: stretch`와 `flex: 1`을 사용해 두 섹션이 항상 동일한 높이를 유지하도록 했습니다. 내용이 적은 쪽이 늘어나는 방식입니다.

---

## 3. 프로덕트 등록 (`product-register.html`)

### 반응형 레이아웃 설계 의도

**의도**: 광고 운영자는 다양한 화면 환경에서 작업합니다. 기존 `max-width: 1000px` 제한은 넓은 모니터에서 좌우 여백이 과도하게 발생해 화면 공간을 낭비했습니다. 특히 미디어 선택 섹션처럼 지도와 리스트를 나란히 봐야 할 때 공간 활용이 중요합니다.

- `max-width` 제거 → 넓은 화면에서는 더 많은 정보를 가로로 표시
- `≤ 900px`: 2열 폼 → 1열 (좁은 화면에서 입력 필드가 너무 작아지는 것 방지)
- `≤ 700px`: 미디어 섹션 단일 컬럼 (지도와 리스트를 세로로 쌓음)

### 광고 옵션 그리드 설계 의도

**의도**: 광고 옵션(일반광고, 시보광고, 날씨 연동 등)은 체크박스 선택 항목들입니다. 기존 상하 나열 방식은 화면이 넓어도 좁은 단일 열로만 표시되어 공간을 비효율적으로 사용했습니다.

`auto-fill` + `minmax(220px, 1fr)` 그리드를 사용해 화면 너비에 따라 자동으로 열 수가 조정됩니다. 옵션 항목들이 카드처럼 시각적으로 구분되면서도, 좁은 화면에서는 자연스럽게 줄바꿈됩니다.

### 미디어 선택 섹션 레이아웃 설계 의도

**의도**: 미디어를 선택할 때 사용자는 두 가지 정보를 동시에 참조합니다.
- **리스트**: 미디어의 스펙(운영시간, 사이클 등)을 비교하고 체크
- **지도**: 선택한 미디어의 물리적 위치를 확인

이 두 정보는 동등한 중요도를 가집니다. `1:1` 그리드로 동일한 너비를 부여하고, 높이도 `420px`로 동일하게 맞춰 시각적 균형을 유지했습니다.

### 미디어 리스트 테이블 형식 설계 의도

**의도**: 카드형 레이아웃은 각 미디어 항목이 이름, 주소, 스펙 4개, 센서 배지를 세로로 쌓아 항목당 높이가 컸습니다. 미디어가 10개 이상이면 스크롤이 과도하고, 미디어 간 스펙 비교가 어렵습니다.

테이블로 전환하면:
- **비교 용이성**: 같은 열에서 여러 미디어의 운영시간, 사이클을 한눈에 비교
- **정보 밀도**: 동일 공간에 더 많은 미디어 표시
- **선택 편의성**: 행 전체 클릭으로 선택/해제, sticky 헤더로 스크롤 시에도 컬럼명 유지

| 컬럼 | 포함 이유 |
|---|---|
| 미디어명 + 주소 | 미디어 식별을 위한 핵심 정보 |
| 설치유형 | 광고 소재 제작 방향(가로/세로형)에 영향 |
| 운영시간 | 광고가 실제로 노출되는 시간대 파악 |
| 사이클 | 한 사이클 내 최대 광고 재생 시간 |
| 기준슬롯 | 슬롯 단가 및 보장 횟수 계산의 기준 |
| 센서 | 동적 광고(날씨·타겟·시간 연동) 가능 여부 |
| 상태 | 현재 운영 중인 미디어인지 즉시 확인 |

### 핀 호버 툴팁 설계 의도

**의도**: 지도에서 핀만 보면 어떤 미디어인지 알 수 없습니다. 클릭 전에 미리 확인할 수 있는 경량 프리뷰가 필요합니다.

사진을 아래에, 이름과 주소를 위에 배치한 이유는 **먼저 텍스트로 식별한 후 사진으로 시각 확인**하는 자연스러운 시선 흐름을 따르기 위해서입니다. 사진이 위에 있으면 시선이 이미지에 먼저 가고, 이름을 찾기 위해 다시 올라가야 합니다.

---

## 4. 프로덕트 상세 (`product-detail.html`)

### 포함 미디어 카드 설계 의도

**의도**: 프로덕트 상세 페이지에서 포함된 미디어를 확인할 때, 단순한 목록 나열로는 각 미디어의 운영 상태와 스펙을 파악하기 어렵습니다.

**좌측 상태 보더(3px colored border)**를 사용한 이유는 스크롤 없이 카드 좌측만 훑어봐도 온라인/오프라인 미디어를 즉시 구분할 수 있게 하기 위해서입니다. 상태 정보를 텍스트나 배지로만 표현하면 시선이 카드 내부까지 들어가야 합니다.

**"평균 슬롯 사용률"** 라벨은 실시간 점유율이 아닌 일정 기간의 평균값임을 명확히 전달하기 위해 "평균"을 명시했습니다. 단순 "슬롯 사용률"은 현재 점유율로 오인될 수 있습니다.

**편성표 링크를 카드 푸터에 배치**한 이유는 미디어 정보를 확인한 직후 해당 미디어의 편성 현황으로 자연스럽게 이동할 수 있는 흐름을 만들기 위해서입니다.

---

## 5. 편성표 공통 설계

### 헤더 바에 컨텍스트 정보 + 드롭다운 배치 의도

**의도**: 기존 편성표 페이지는 어떤 미디어/프로덕트의 편성표를 보고 있는지 명확하지 않았습니다. 달력과 스케줄 정보만 있고, 지금 내가 "무엇"을 보고 있는지 알려주는 요소가 부족했습니다.

헤더에 미디어명/프로덕트명을 드롭다운으로 배치하면:
1. **컨텍스트 명시**: 어떤 미디어/프로덕트의 편성표인지 항상 상단에서 확인 가능
2. **즉시 전환**: 다른 미디어/프로덕트의 편성표로 이동 시 뒤로 가지 않고 드롭다운에서 바로 선택
3. **드릴다운 구조 완성**: 상위 페이지에서 선택한 미디어/프로덕트의 맥락이 편성표까지 이어짐

`appearance: none`으로 select의 기본 스타일을 제거하고 헤더 폰트와 동일한 크기로 스타일링해, 드롭다운이 헤더 정보처럼 자연스럽게 녹아들게 했습니다.

### 헤더 상단 여백(`padding-top: 36px`) 의도

**의도**: 편성표는 달력 + 스케줄 패널로 정보가 밀집된 페이지입니다. 여백 없이 콘텐츠가 GNB 바로 아래에 붙으면 시각적으로 답답합니다. 36px 여백으로 호흡을 주어 사용자가 페이지에 진입했을 때 헤더 정보를 여유 있게 인식할 수 있게 했습니다.

### 달력 날짜 호버 툴팁 설계 의도

**의도**: 월간 편성표에서 달력 셀에 표시되는 색상 바는 스케줄의 존재를 나타내지만, 구체적으로 어떤 스케줄이 몇 개 편성되어 있는지 알 수 없습니다. 클릭해야만 우측 패널에서 확인할 수 있어 탐색 비용이 높습니다.

**호버 툴팁**으로 클릭 전에 스케줄 목록을 미리 보여주면, 사용자는 "이 날짜에 뭐가 있더라?"를 빠르게 스캔하면서 원하는 날짜를 찾을 수 있습니다. 스케줄이 4개 이상인 경우 "외 N개"로 접어 툴팁이 과도하게 커지지 않게 했습니다.

### 선택 날짜 Fill 처리 설계 의도

**의도**: 선택된 날짜를 테두리(stroke)로 표현하면 달력 셀의 기존 격자 선과 겹쳐 구분이 약합니다. 반면 셀 내부를 옅은 회색(`#DEDEDE`)으로 채우면 선택 상태가 더 직관적으로 전달되면서, 스케줄 색상 바와도 시각적으로 충돌하지 않습니다.

### 우측 스케줄 패널 중복 정보 제거 의도

**의도**: 미디어/프로덕트 정보가 헤더와 우측 패널 두 곳에 동시에 표시되면, 같은 정보를 두 번 읽게 됩니다. 헤더로 통합하고 패널에서 제거함으로써 패널 공간을 스케줄 리스트에 온전히 할애할 수 있습니다.

---

## 6. 일간 편성표

### 시보광고 슬롯 제거 의도

**의도**: 프로덕트 일간 편성표에서 사이클 구간 위에 시보광고 슬롯이 표시되어 있었으나, 이 정보는 우측 스케줄 패널에서 이미 제공됩니다. 중복된 시각 요소가 타임라인을 복잡하게 만들어 삭제했습니다.

---

## 7. 디자인 시스템

### 상태 표현 색상 체계

| 용도 | 색상값 | 의도 |
|---|---|---|
| 온라인 / 활성 | `#58C65D` | 초록 = 정상 운영 중 |
| 오프라인 / 비활성 | `#CED2D6` | 회색 = 비활성 |
| 선택 날짜 fill | `#DEDEDE` | 충분히 옅어 스케줄 바 색상을 가리지 않음 |
| 선택 미디어 행 배경 | `#F0F9F1` | 초록 계열로 선택 상태임을 표시 |
| 비활성 GNB | `opacity: 0.4` | 완전히 숨기지 않고 "존재하지만 접근 불가" 전달 |

### 센서 배지 색상 체계

센서 유형마다 고유한 색상을 부여해, 미디어 리스트를 훑을 때 어떤 동적 광고 기능을 지원하는지 텍스트를 읽지 않고도 색으로 구분할 수 있습니다.

| 센서 | 배경 | 텍스트 | 의미 |
|---|---|---|---|
| 날씨 | `#E8F5FF` | `#1A7CC0` | 날씨 데이터 연동 광고 가능 |
| 타겟 | `#FFF0E8` | `#C05A1A` | 유동인구/타겟 데이터 연동 가능 |
| 시간 | `#F0EEFF` | `#5B3EC0` | 시간대별 광고 분기 가능 |

### 핀 툴팁 레이아웃 원칙

이름·주소(텍스트)를 상단에, 사진을 하단에 배치합니다. 사용자는 먼저 텍스트로 미디어를 식별하고, 그 다음 사진으로 실제 위치를 시각적으로 확인합니다. 이 순서가 역전되면 사진을 먼저 보고 나서 이름을 찾아야 하는 역방향 탐색이 발생합니다.

---

## 8. 파일별 변경 이력 요약

| 파일 | 주요 변경 |
|---|---|
| `assets/css/cms.css` | GNB 비활성 스타일 추가 |
| `pages/product-list.html` | 현황판 카드 높이 일치, GNB 제한 |
| `pages/product-register.html` | 반응형 레이아웃, 광고옵션 그리드, 지도 확대, 미디어 테이블, 핀 툴팁 |
| `pages/product-detail.html` | 미디어 카드 재설계, 핀 툴팁 |
| `pages/product-edit.html` | GNB 제한 |
| `pages/product-playplan.html` | GNB 제한 |
| `pages/schedule-media-month.html` | 헤더 드롭다운, 달력 툴팁, 선택 날짜 fill, padding-top |
| `pages/schedule-product-month.html` | 헤더 드롭다운, 달력 툴팁, 선택 날짜 fill, padding-top, GNB 제한 |
| `pages/schedule-media-day.html` | padding-top |
| `pages/schedule-product-day.html` | padding-top, 시보광고 슬롯 제거 |
| `pages/schedule-cycle-edit.html` | GNB 제한 |
| `pages/schedule-register-form.html` | GNB 제한 |
| `pages/schedule-main.html` | GNB 제한 |
